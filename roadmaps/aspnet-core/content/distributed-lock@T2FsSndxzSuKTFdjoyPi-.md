# Lock Distribuído

Um lock distribuído coordena o acesso a um recurso compartilhado entre vários processos ou serviços. Ele previne modificações concorrentes e condições de corrida garantindo que apenas um cliente tenha o lock em um determinado momento, geralmente suportado por uma loja como Redis, ZooKeeper ou um banco de dados.
