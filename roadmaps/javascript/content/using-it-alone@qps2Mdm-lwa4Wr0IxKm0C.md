# Usando sozinho

Quando `this` é usado fora de qualquer função, ele se refere ao objeto global. No navegador, isso é `window`. No Node.js no nível superior de um módulo, `this` se refere a `module.exports`. Este uso é raramente necessário em código de aplicativo.
